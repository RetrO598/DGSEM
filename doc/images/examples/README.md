# Example Images

Store images used by `doc/examples.md` in this directory.

Use relative Markdown paths from `examples.md`, for example:

```markdown
![2D blast wave](images/examples/blast_wave_hg.png)
```

